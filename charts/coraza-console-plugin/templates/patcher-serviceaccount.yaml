{{- if and (.Values.plugin.patcherServiceAccount.create) (.Values.plugin.jobs.patchConsoles.enabled) -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "coraza-console-plugin.patcherServiceAccountName" . }}
  labels:
    {{- include "coraza-console-plugin.labels" . | nindent 4 }}
  {{- with .Values.plugin.patcherServiceAccount.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
{{- end }}
