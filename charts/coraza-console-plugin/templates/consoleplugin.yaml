apiVersion: console.openshift.io/v1
kind: ConsolePlugin
metadata:
  name: {{ template "coraza-console-plugin.name" . }}
  labels:
    {{- include "coraza-console-plugin.labels" . | nindent 4 }}
spec:
  displayName: {{ default (printf "%s Plugin" (include "coraza-console-plugin.name" .)) .Values.plugin.description }}
  i18n:
    loadType: Preload
  backend:
    type: Service
    service:
      name: {{ template "coraza-console-plugin.name" . }}
      namespace: {{ .Release.Namespace }}
      port: {{ .Values.plugin.port }}
      basePath: {{ .Values.plugin.basePath }}
