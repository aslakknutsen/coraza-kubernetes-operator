apiVersion: v1
kind: Service
metadata:
  {{- if not .Values.plugin.certificateSecretName }}
  annotations:
    service.alpha.openshift.io/serving-cert-secret-name: {{ template "coraza-console-plugin.certificateSecret" . }}
  {{- end }}
  name: {{ template "coraza-console-plugin.name" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "coraza-console-plugin.labels" . | nindent 4 }}
spec:
  ports:
    - name: {{ .Values.plugin.port }}-tcp
      protocol: TCP
      port: {{ .Values.plugin.port }}
      targetPort: {{ .Values.plugin.port }}
  selector:
    {{- include "coraza-console-plugin.selectorLabels" . | nindent 4 }}
  type: ClusterIP
  sessionAffinity: None
